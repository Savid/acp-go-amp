---
title: Sessions
description: The mental model for how ACP sessions map onto Amp threads and move through their lifecycle.
---

An ACP session wraps at most one Amp thread. The adapter mints its own UUID
session IDs; the native thread ID (`T-...`) is created by the session's first
prompt turn and recorded in the manifest as `nativeSessionId`. A session that
is never prompted never owns a server-side thread. Durable state lives on the
Amp server side; the adapter keeps only a local mirror for display and
continuity.

## Lifecycle

- `session/new` mints the session id and persists an initial manifest, then
  returns the select config option for `mode`. It may run bounded native
  readiness probes, but no prompt or thread-creating command runs, so no
  server-side thread exists until the first prompt.
- The first `session/prompt` runs a thread-less `amp -x` execute: amp creates
  the thread, reports its id on the init frame, and the adapter records it in
  the manifest immediately. Later prompts run `amp threads continue` against
  the recorded thread.
- `session/load` reads the store manifest and transcript, verifies that any
  recorded server-side Amp thread can still be addressed, recreates the
  adapter session with a fresh isolated native home, and replays stored frames
  to the client.
- `session/resume` performs the same continuability check without replaying
  history.
- `session/delete` writes the store tombstone first, closes any active local
  session, then calls `amp threads delete` when the manifest records a native
  thread; a session with no recorded thread deletes locally only. A native
  delete failure is returned after the local session is already hidden from
  list/load/resume, and native cleanup can be retried while the isolated local
  operation state is intact. If embedded shutdown retries the native deletion
  and it still fails, shutdown releases the isolated home and permanently
  retains the classified deletion failure; a later `Agent.Close` neither
  reports success nor issues a native command from state already released. If
  the store refuses the tombstone, an active session remains live and visibly
  mirror-unsynced; the failed delete cannot report a fenced persistence attempt
  as success. A successful tombstone discharges that owed commit rather than
  writing frames back over the deleted row.
- `session/close` stops accepting new prompts for the local session, commits any
  frames a failed store write left unsynced, and removes its isolated native
  home. Its persistence fence joins writes already admitted before close and
  rejects later ordinary/config writes; no store write or config notification
  can outlive the response. A close that cannot commit or remove the home fails,
  retains the exact local session, and retries only the incomplete rung. It does
  not delete the server-side Amp thread.

## Server-side state and the local mirror

Because state is server-side, the adapter cannot rebuild a thread from local
history. The transcript mirror and canonical image records exist to replay
display updates on load — not to recreate native state. Continuation always
requires the live server-side Amp thread and `AMP_API_KEY`; if the thread is
gone, load still replays local history but the next prompt returns a terminal
`-32603` `{"error": "amp_internal_failure", "class": "native_state_missing"}`.
See [Session store](/docs/features/session-store) for the durability boundary and
[ACP methods](/docs/reference/acp-methods#internal-errors-away-from-a-prompt-turn)
for the closed token vocabulary.

## Mirror commit ordering

Every settled turn publishes its frames to the store at one commit point: a
cancelled, failed, or timed-out turn commits what it already streamed, exactly
as a clean `end_turn` does, so the local mirror never holds less than the client
was shown while the server-side thread advances. The commit runs after the
turn's process containment boundary completes and before the terminal state the
host sees. If a store write fails after a settled turn, the adapter keeps the
exact frames in memory and blocks the next prompt with a `mirror_unsynced`
error until the mirror is reconciled, so the local record never silently drifts
from what Amp produced. `session/close` reconciles it too: the close retries the
retained frames before the session is reclaimed and fails when it cannot, so the
request that ends a session is never the one that drops a settled turn. A
session already fenced for `session/delete` writes nothing there — the tombstone
is the last write to that row, and the frames stay retained until a successful
tombstone discharges them. `Agent.Close` reconciles the mirror too, for the same
reason and with the same failure: an embedded host's shutdown must not silently
drop what a wire close would have committed. Shutdown is the last callable
boundary, so it boundedly joins and contains every owned wrapper, releases local
state where containment permits, reports any refused rung, removes every
callable ownership path, and memoizes that exact result. A later `Agent.Close`
observes the result; it is not a retry protocol.
