# Session Store

The store format is `amp-thread-mirror-v1`. The main subpath contains the latest manifest and `transcript` contains appended Amp stream JSON frames.

The mirror supports load replay for display. Native continuation still depends on the server-side Amp thread existing.
